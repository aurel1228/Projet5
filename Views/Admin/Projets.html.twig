{% set titre = "page des projets" %}
{% include "/Admin/_headAdmin.html.twig"  %}
   <body>
      <div id="utilisateur">
         {% include "/_Info.html.twig" %}
         <a href="/">retour accueil</a>
         <a href="/Admin/users">retour admin</a>
         {% include "/_logoutButton.html.twig" %}
      </div>      

      <div id="titreprojet"><h1>Liste des Projets :</h1></div>

      <table id="tableauProjets">
         <thead>
            <tr>
               <th scope="col">Lien vers le projet</th>
               <th scope="col">Numéro du projet</th>
               <th scope="col">Modifier</th>
               <th scope="col">Supprimer</th>
               <th scope="col">Icône</th>
            </tr>
         </thead>
         <tbody>
            {% for projet in Projets %}
                <tr>
                <td>{{ projet.lien }}</td>
                <td>{{ projet.ordre}}</td>
                <td><a href="/Admin/ProjetsModifier?id={{ projet.id }}">modifier</a></td>
                <td><a href="/Admin/ProjetsDelete?id={{ projet.id }}">supprimer</a></td>    
                <td>{% if projet.icon is not null %}<img src="/images/icon/{{ projet.icon }}" alt="image de l'icon">{% endif %}</td> 
                </tr> 
            {% endfor %} 
         </tbody>
      </table>

      <div id="validation">{{ SESSION['message']|default("") }}</div>
      <div id="projetlink"><a href="/Admin/ProjetsModifier?id=0">ajouter un nouveau projet</a></div>

   </body>
</html>