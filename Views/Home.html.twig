{% set titre = "projet5 Accueil" %}
{% include "/_head.html.twig"  %}
   <body>
      {% if SESSION['role'] is not empty %}
         <div id="utilisateur">
            {% include "/_info.html.twig" %}
            {% if SESSION['role'] == "admin" %}
               <a href="/Admin/Users" id="backadmin">page admin</a>
            {% endif %}
            {% include "/_logoutButton.html.twig" %}
         </div>
      {% endif %}
      
      <div class="container">
			<nav>
				<ul>
					<li><a href="#container">Accueil</a></li>
					<li><a href="#list-projet">Portfolio</a></li>
               {% if SESSION['role'] is empty %}
                  <li><a href="#connexion">Connexion</a></li>
               {% endif %}
					<li><a href="#meteo">Météo</a></li>
				</ul>
			</nav>

         <section class="site-container">
				<p>Bienvenue sur mon portfolio Projet 5</p>
				<h1>Voici les projets que j'ai réalisé</h1>

				<div class="list-projet">
                Cliquez pour voir les différents projets ci-dessous: 
               <div class="column">

                  {% for projet in Projets|sort((a, b) => a.ordre <=> b.ordre) %}
                     <a href="{{ projet.lien }}" target="_blank"><img class="imgprojet" src="/images/icon/{{ projet.icon }}" alt="image du projet{{ projet.ordre}}"></a>
                  {% endfor %} 
                  
               </div>          
				</div>  
            sites réalisés dans le cadre d'un parcours Openclassrooms
			</section>

         {% if SESSION['pseudo'] is empty %}
            
            <section class="login-container">
               <form id="connexion" method="post">
                  <h1 id="login-title">Formulaire de connexion</h1>

                  <label id="pseudotext" for="pseudo">Nom utilisateur</label>
                  <input type="text" id="pseudo" name="pseudo" placeholder="pseudo" required >

                  <label id="passwordtext" for="password">mot de passe</label>
                  <input type="password" id="password" name="password" placeholder="mot de passe" required>

                  <button id="login-button" type="submit" name="connexion" value="1">connexion</button>
               </form>
            </section>
         {% endif %}
         {{ message|default("") }}
         {{ SESSION['message']|default("") }}

         <section class="meteo-container">
            <form id="meteoform" method="post">

               <label id="meteotext" for="meteo">Voir météo de la ville ci-dessous</label>
               <input type="text" id="meteo" name="cityName" placeholder="Entrez nom de la ville" required>

               <button type="button" id="search-btn" name="CheckCity">Validez</button>
               <div id="error"></div>
            </form>
            
            
            <div id="apimeteo"></div>
            <div id="reponse">   
               <div id="temp"></div><div id="ressenti"></div>
               <div id="speed"></div> 
               <div id="desc"></div>
            </div>   
            
         </section>
      </div>     

      <script src="/js/trustedtypes.api_only.build.js"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/dompurify/3.2.7/purify.min.js" integrity="sha512-78KH17QLT5e55GJqP76vutp1D2iAoy06WcYBXB6iBCsmO6wWzx0Qdg8EDpm8mKXv68BcvHOyeeP4wxAL0twJGQ==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
      <script src="/js/Home.js"></script>
   </body>
</html>